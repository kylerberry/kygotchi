$('document').ready(function() {
  Kygotchi.init({
    'gotchi' : '#gotchi',
    'bindings' : {
      'feed' : '#feed',
      'play' : '#play',
      'reset': '#reset',
      'sleep' : '#sleep',
      'wake' : '#wake',
      'medicine' : '#medicine'
    }
  });
});